interface:
  display_name: "Message Use"
  short_description: "Search and organize local macOS Messages"
  default_prompt: "Use $message-use to query chat.db for every read-only lookup without opening Messages.app; use the app only for explicitly requested UI or state changes, and never send replies."
