interface:
  display_name: "Claude Use"
  short_description: "Delegate tasks to the local Claude Code CLI"
  default_prompt: "Use $claude-use to delegate the task to local Claude Code CLI, then review the result and summarize the final state."
policy:
  allow_implicit_invocation: false
