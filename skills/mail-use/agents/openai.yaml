interface:
  display_name: "Mail Use"
  short_description: "Search, summarize, and organize local Mail.app email"
  default_prompt: "Use $mail-use to query the local Mail cache for every read-only lookup without opening Mail.app; use app automation only for explicitly requested UI or state changes."
